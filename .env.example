# PWA build-time config. NO secrets here — the GitHub PAT lives only as a Worker secret.
# Copy to .env.local and set your deployed Worker URL.
# For local development point this at `npm run dev:worker` (defaults to http://127.0.0.1:8787).
VITE_WORKER_URL=https://life-task-api.YOUR-SUBDOMAIN.workers.dev
